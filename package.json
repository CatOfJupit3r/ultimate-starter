{
  "name": "startername",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "docker compose -f ./docker-compose.dev.yml up -d && pnpm --parallel --filter=server --filter=web --filter=@startername/shared run dev",
    "build": "pnpm --recursive run build",
    "test": "pnpm --recursive --stream run test",
    "check-types": "pnpm --recursive --stream --no-bail run check-types",
    "prettify": "pnpm --recursive --stream run prettify",
    "prettier": "pnpm run prettify",
    "dev:web": "pnpm --filter=web run dev",
    "dev:server": "pnpm --filter=server run dev",
    "dev:shared": "pnpm --filter=@startername/shared run dev",
    "lint": "pnpm --recursive --stream run lint",
    "lint:fix": "pnpm run lint",
    "clean:modules": "tsx scripts/clean.ts",
    "prepare": "husky",
    "commit": "cz",
    "build:deps": "pnpm --filter=@startername/shared run build",
    "update:workspaces": "pnpm update --interactive --recursive"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "commitizen": "4.3.1",
    "cz-git": "1.12.0",
    "husky": "9.1.7",
    "lint-staged": "16.2.7",
    "rimraf": "6.1.2",
    "tsx": "catalog:"
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-git",
      "czConfig": "./cz.config.cjs"
    }
  },
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": "24.13.0",
    "pnpm": ">=10.0.0"
  }
}
